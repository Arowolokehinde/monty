#include "monty.h"
/**
 * pe_stack - Function that prints the top
 * @stack: points to the stack head
 * wriiten by Pelile
 * @number: The line_number
 * Return: Always success
*/
void pe_stack(stack_t **stack, unsigned int number)
{
	(void)stack;
	(void)number;
	carry.flag = 0;
}
