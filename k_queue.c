#include "monty.h"

/**
 * k_queue - function that prints the top
 * @stack: points to the stack head
 * @number: The line_number
 * Return: Always success
*/

void k_queue(stack_t **stack, unsigned int number)
{
	(void)stack;
	(void)number;
	carry.flag = 1;
}
